import dotenv from 'dotenv';
dotenv.config();

export const base_url=process.env.BASE_URL;
export const tenant_id=process.env.TENANT_ID;
export const tenant_token=process.env.TENANT_TOKEN;
export const business_unit_id=process.env.BUSINESS_UNIT_ID;
